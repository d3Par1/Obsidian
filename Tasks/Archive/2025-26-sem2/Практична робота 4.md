---
course: АСПЗ 2026
type: практика
status: done
year: 2
semester: 2
period: 2025-26
---
# Практична робота 4

**Курс:** АСПЗ 2026

## Вкладення

- 📎 Практична робота 4

## Google Classroom

[Відкрити завдання](https://classroom.google.com/u/0/c/ODQyNDY3MjgwMTkz/a/ODQyOTYxOTc5NTQ1/details)

---

## 📄 Практична робота 4

Практична робота 4
Завдання ЗАГАЛЬНЕ ДЛЯ ВСІХ
Завдання 4.1
Скільки пам’яті може виділити malloc(3) за один виклик?
Параметр malloc(3) є цілим числом типу даних size_t, тому логічно максимальне число, яке можна передати як параметр malloc(3), — це максимальне значення size_t на платформі (sizeof(size_t)). У 64-бітній Linux size_t становить 8 байтів, тобто 8 * 8 = 64 біти. Відповідно, максимальний обсяг пам’яті, який може бути виділений за один виклик malloc(3), дорівнює 2^64. Спробуйте запустити код на x86_64 та x86. Чому теоретично максимальний обсяг складає 8 ексабайт, а не 16?
Завдання 4.2
Що станеться, якщо передати malloc(3) від’ємний аргумент? Напишіть тестовий випадок, який обчислює кількість виділених байтів за формулою num = xa * xb. Що буде, якщо num оголошене як цілочисельна змінна зі знаком, а результат множення призведе до переповнення? Як себе поведе malloc(3)? Запустіть програму на x86_64 і x86.
Завдання 4.3
Що станеться, якщо використати malloc(0)? Напишіть тестовий випадок, у якому malloc(3) повертає NULL або вказівник, що не є NULL, і який можна передати у free(). Відкомпілюйте та запустіть через ltrace. Поясніть поведінку програми.
Завдання 4.4
Чи є помилки у такому коді?
void *ptr = NULL;
while (&lt;some-condition-is-true>) {
    if (!ptr)
        ptr = malloc(n);
    [... <використання 'ptr'> ...]
    free(ptr);
}


Напишіть тестовий випадок, який продемонструє проблему та правильний варіант коду.
Завдання 4.5
Що станеться, якщо realloc(3) не зможе виділити пам’ять? Напишіть тестовий випадок, що демонструє цей сценарій.
Завдання 4.6
Якщо realloc(3) викликати з NULL або розміром 0, що станеться? Напишіть тестовий випадок.
Завдання 4.7
Перепишіть наступний код, використовуючи reallocarray(3):
struct sbar *ptr, *newptr;
ptr = calloc(1000, sizeof(struct sbar));
newptr = realloc(ptr, 500*sizeof(struct sbar));


Порівняйте результати виконання з використанням ltrace.
Завдання по ВАРІАНТАХ


1. Реалізувати власний malloc/free на базі brk() для малих блоків і mmap() для великих. Підтримати розбиття та об’єднання вільних блоків. Показати вплив фрагментації.
2. Написати програму з поступовим витоком пам’яті. Підтвердити його через Valgrind, /proc/self/status та власний лічильник алокацій.
3. Дослідити продуктивність malloc/free при 1, 2, 4, 8 потоках. Зробити варіант із thread-local пулом і порівняти час.
4. Експериментально показати, як часто після free() malloc() повертає ту саму адресу для різних розмірів блоків.
5. Використати mmap() та mprotect() для створення сторожових сторінок і продемонструвати гарантований segfault при виході за межі буфера.
6. Виділити великий обсяг пам’яті, виміряти різницю між VmSize і VmRSS до та після запису в сторінки. Пояснити lazy allocation.
7. Реалізувати тест із різним stride доступу до масиву (1, 4, 16, 64, 4096 байт) і порівняти час виконання. Пояснити вплив кешу та TLB.
8. Порівняти calloc() і malloc()+memset() для великих блоків, виміряти час та пояснити вплив zero-page.
9. Створити власний memory pool для об’єктів фіксованого розміру. Порівняти швидкість із стандартним malloc.
10. Дослідити поведінку realloc() при розширенні блоку. Виявити випадки копіювання та “розширення на місці”.
11. Створити програму, що викликає контрольований OOM через обмеження ulimit, та дослідити реакцію malloc.
12. Перевірити, як змінюється program break через sbrk(0) після серії малих алокацій.
13. Порівняти алокацію великого блоку (200 МБ) і багатьох малих (200 000 × 1 КБ) через /proc/self/maps.
14. Дослідити, як fork() впливає на використання пам’яті після великої алокації. Показати ефект Copy-On-Write.
15. Перехопити malloc/free через обгортку та вести журнал усіх виділень. Виявити незвільнені блоки.
16. Створити сценарій фрагментації: чергування великих і малих алокацій, часткове звільнення. Проаналізувати, чи можлива алокація великого суцільного блоку.
17. Порівняти продуктивність aligned_alloc() і malloc() для масиву чисел із вимірюванням часу обчислень.
18. Дослідити, чи завжди malloc(0) повертає NULL або унікальну адресу, і чи безпечно її звільняти.
19. Запустити програму під Valgrind із помилкою use-after-free та проаналізувати звіт.
20. Порівняти поведінку malloc у двох різних реалізаціях (glibc та musl) на однакових тестах продуктивності й фрагментації.
