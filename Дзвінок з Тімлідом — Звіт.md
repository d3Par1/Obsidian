# Дзвінок з Тімлідом — Звіт (R&D Engineer · БПЛА)

> [!info] Як користуватись
> Це **шаблон-звіт**. Розділи 1–5 заповнюй **одразу після дзвінка**, поки свіжо в пам'яті (запиши реальні питання, свої відповіді, домовленості). Розділи 6–7 — **шпаргалка перед дзвінком**: швидко освіжити C/C++ базу (з твоїх практичних завдань) та embedded/сенсори.
>
> 🔗 Підготовка: [[Дзвінок з Тімлідом — Підготовка]] · Глибокі відповіді: [[Interview Prep Master]] · Резюме: [[Embedded CV]]

---

## 🗂️ Метадані дзвінка

| Поле | Значення |
|------|----------|
| 📅 Дата / час | ✍️ |
| 👤 Тімлід (ім'я, посада) | ✍️ |
| 🏢 Компанія / роль | R&D Engineer · БПЛА ✍️ |
| 💻 Формат | Дзвінок (✍️ Zoom / Meet / Telegram) |
| ⏱️ Тривалість | ✍️ |
| 🎯 Загальне враження (1–10) | ✍️ |

---

# 1. ❓ Питання тімліда + мої відповіді

> Записуй **дослівно питання** і **як ти відповів**. Познач: ✅ відповів упевнено · ⚠️ плавав · ❌ не знав.

| # | Питання тімліда | Як я відповів | Оцінка |
|---|-----------------|----------------|--------|
| 1 | ✍️ Розкажіть про себе | ✍️ | ✅/⚠️/❌ |
| 2 | ✍️ | ✍️ | |
| 3 | ✍️ | ✍️ | |
| 4 | ✍️ | ✍️ | |
| 5 | ✍️ | ✍️ | |

> [!note] Питання, на які я «поплив» (підтягнути!)
> - ✍️
> - ✍️

---

# 2. 🔧 Технічні теми, які зачепили

> Які теми обговорювали і наскільки глибоко. Постав ✍️ біля тих, де треба підтягнути.

- [ ] C / C++ база (вказівники, stack/heap, ООП) — ✍️
- [ ] Embedded / мікроконтролери (STM32, ESP32, GPIO) — ✍️
- [ ] Дрони (flight controller, PID, ArduPilot/PX4) — ✍️
- [ ] MAVLink / телеметрія — ✍️
- [ ] Сенсори (IMU, GPS, баро) — ✍️
- [ ] Python / ROS2 — ✍️
- [ ] Інше: ✍️

**Конкретні технічні питання, які запам'ятав:**
> ✍️

---

# 3. 🙋 Мої питання + відповіді тімліда

> З Розділу 9 підготовки. Запиши, **що відповіли** — це важливо для рішення.

| Моє питання | Відповідь тімліда |
|-------------|-------------------|
| Як виглядає типовий тиждень junior-інженера? | ✍️ |
| На яких задачах працюватиму в перші 1–3 міс? | ✍️ |
| Який стек / прошивки (ArduPilot, PX4, власна)? | ✍️ |
| Чи буде ментор на старті? | ✍️ |
| Графік з урахуванням навчання (гібрид)? | ✍️ |
| Які наступні етапи відбору? | ✍️ |

---

# 4. ✅ Наступні кроки + висновки

**Домовленості / умови:**
- Наступний крок: ✍️ (тестове / співбесіда / офер / чекаю)
- Дедлайн / коли відповідь: ✍️
- Графік, формат, оплата (якщо обговорювали): ✍️

**Мої висновки:**
> [!success] Що пройшло добре
> - ✍️

> [!warning] Що підтягнути до наступного етапу
> - ✍️

**To-do після дзвінка:**
- [ ] Подякувати за дзвінок (повідомлення)
- [ ] ✍️
- [ ] ✍️

---
---

# 6. 🧠 ШПАРГАЛКА: C / C++ база (з моїх практичних завдань)

> Освіжити перед дзвінком. Це те, що я **реально набирав і запускав** — можу впевнено пояснити. (папка `code-typing-practice`)

### Основи й цикли
- **Змінна** — іменована комірка пам'яті; **тип** задає розмір і вид даних (`int`, `float`, `char`).
- **Чому цикл `for (int i = 0; i < 10; i++)` рахує 0..9?** Умова `i < 10` перевіряється **перед** кожним проходом; коли `i` стає 10 — `10 < 10` хибне, цикл зупиняється, **не** друкуючи 10. `<` зупиняє **перед** числом, `<=` — **на** числі.
- **Вкладені цикли** — зовнішній крутиться раз на рядок, внутрішній повністю на кожен прохід → сітки, таблиці (3×3 = 9 проходів).

### Функції
- **Функція повертає значення**, процедура (`void`) — ні, метод — функція всередині класу.
- **Параметр** — у оголошенні; **аргумент** — конкретне значення при виклику.

### Вказівники й пам'ять (ключове для embedded!)
- **Вказівник** — змінна, що зберігає **адресу** іншої змінної. `int *p = &x;` (`&` = адреса, `*` = значення за адресою).
- **`arr[i]` ≡ `*(arr + i)`** — індексація це просто арифметика вказівників.
- **Stack** — локальні змінні; швидко, звільняється сам при виході з функції. **Heap** — `malloc`/`new`; звільняєш сам (`free`/`delete`), ризик витоків.
- **Правило: `delete` лише те, що з `new`.** `delete &stackObj` = краш. В embedded heap часто **уникають** (фрагментація, недетермінований час).
- **Smart pointer** (`unique_ptr`) — heap з **автоматичним** звільненням, без ручного `delete`.

### ООП (база з курсу ООА)
- **Клас** — креслення (дані + методи); **об'єкт** — екземпляр. `Motor leftMotor;`
- **3 кити:** інкапсуляція (`private`/`public`), наслідування (`Dog : Animal`), поліморфізм (`virtual` методи — один інтерфейс, різна поведінка).
- **`struct`** — це клас лише з даними; C++ клас = struct + методи.

### Алгоритми (можу написати на папері)
- **Bubble sort** — міняю сусідів місцями, поки не відсортовано (вкладені цикли + swap `temp=a; a=b; b=temp;`).
- **Selection sort** — шукаю мінімум, ставлю наперед, повторюю.
- **Реально** ж сортую `qsort` (C) або `std::sort` (C++) — не пишу вручну.

### Компіляція C/C++
1. **Препроцесор** (`#include`, `#define`) → 2. **Компіляція** (`.c` → `.o`) → 3. **Лінкування** (`.o` + бібліотеки → `.exe`/прошивка).

---

# 7. ⚙️ ШПАРГАЛКА: Embedded — сенсори, підключення, взаємодія

> «З основ»: який сенсор що міряє, **як його фізично підключити**, і **як з ним спілкуватись у коді**.

### Які сенсори і що міряють
| Сенсор | Що міряє | Навіщо в дроні |
|--------|----------|----------------|
| **Accelerometer** | лінійне прискорення (де «низ»/гравітація) | визначити нахил/орієнтацію |
| **Gyroscope** | кутову швидкість обертання | стабілізація, швидкі корекції |
| **Magnetometer** | напрямок (компас) | курс (heading) |
| **Barometer** | тиск → висоту | утримання висоти |
| **GPS** | позицію (lat/lon/alt) | навігація, waypoints |
| **IMU** | accel + gyro (+ часто mag) в одному чіпі | основа стабілізації |

### Як підключити — 3 основні шини
> Сенсор з'єднується з мікроконтролером (STM32/ESP32) по одній із цих шин. **Завжди ще GND (земля) + живлення 3.3В/5В.**

- **I²C** — 2 дроти: **SDA** (дані) + **SCL** (такт). Багато пристроїв на одних дротах, у кожного своя **адреса** (напр. `0x68` у MPU-6050). Потрібні **pull-up** резистори на SDA/SCL. Найчастіше для IMU/баро/компаса.
- **SPI** — 4 дроти: **MOSI**, **MISO**, **SCK** (такт), **CS** (вибір чіпа). Швидше за I²C, по одному CS на пристрій. Для швидких IMU, SD-карт, дисплеїв.
- **UART** — 2 дроти: **TX** ↔ **RX** (перехрестя: TX одного → RX іншого). «Точка-точка». Класично для **GPS** і телеметрії.

```
MCU (STM32)            Сенсор (IMU по I2C)
   3.3V ───────────────── VCC
   GND  ───────────────── GND
   SDA  ──────┬────────── SDA
   SCL  ──────┼─┬──────── SCL
              │ │
            pull-up резистори до 3.3V
```

### Як взаємодіяти у коді (з основ)
1. **Ініціалізація шини** — налаштувати I²C/SPI/UART (швидкість, піни).
2. **«Рукостискання»** — прочитати `WHO_AM_I` регістр сенсора → переконатись, що відповідає правильним ID (сенсор живий і підключений).
3. **Конфіг** — записати в регістри налаштування (діапазон, частота вибірки, вийти зі сну — багато IMU стартують у sleep).
4. **Зчитування даних** — читати регістри з вимірами (зазвичай 2 байти на вісь: HIGH + LOW → одне 16-бітне число).
5. **Обробка** — перевести «сирі» числа у фізичні одиниці (g, °/с) за scale factor з datasheet.

```c
// Псевдо-приклад (I2C, спрощено):
i2c_init();                          // 1. підняти шину
id = i2c_read_reg(0x68, WHO_AM_I);   // 2. перевірка зв'язку
i2c_write_reg(0x68, PWR_MGMT, 0x00); // 3. розбудити IMU
int16_t ax = i2c_read16(0x68, ACCEL_X); // 4. зчитати вісь X
float g = ax / 16384.0;              // 5. у одиниці g (scale з datasheet)
```

### Polling vs Interrupt (як саме «слухати» сенсор)
- **Polling** — у циклі постійно питаю «є нові дані?». Просто, але марнує CPU.
- **Interrupt** — сенсор сам **сигналить** по окремому піні (`INT`), коли є нові дані → CPU реагує миттєво, не опитуючи. Ефективніше для польотного циклу.

### Як це дебажити (без класичного дебагера)
- **Serial print** (`printf` по UART) — найпростіше, дивлюсь вивід у терміналі.
- **LED** — блимаю в точках коду.
- **Logic analyzer / осцилограф** — бачу реальні сигнали SDA/SCL на пінах.
- **ST-Link (SWD)** — breakpoint'и, регістри.

> [!tip] Якщо сенсор «не відповідає»
> Перевір по черзі: живлення (3.3 vs 5В!) → GND спільна → pull-up на I²C → правильна адреса → чи розбуджений сенсор (PWR регістр). 90% проблем тут.

---

> [!success] Головне про звіт
> Заповни 1–5 **гарячими слідами** після дзвінка — за тиждень деталі забудуться. Розділи 6–7 — твоя міні-база, щоб на дзвінку говорити впевнено саме про те, що **реально робив руками**.
