---
title: Співбесіда — Embedded UAV (Smart Solutions)
tags: [job-search, interview, embedded]
---

> [!info] Контекст
> **Вакансія:** Embedded Software Engineer (виробник БПЛА, м. Київ, офіс)
> **Агенція:** Smart Solutions — рекрутер **Катерина Ткачук**
> **Формат:** технічний скринінг C/C++ + мікроконтролери. Готуйся 20 хв перед дзвінком.
> Глибші теми — у [[Interview Prep Master]]. База — у [[Дзвінок з Тімлідом — Підготовка]].

---

## 🎯 30-секундна самопрезентація (під цю вакансію)

> «Я студент 2 курсу Інженерії ПЗ в КПІ. Маю впевнену основу в C/C++ і Python, практикувався з ESP32 та Raspberry Pi Pico W — писав драйвери периферії, працював з I²C/SPI/UART, сенсорами (BMP280, IMU, OLED), serial-відладкою. Зараз активно вивчаю FreeRTOS і STM32 bare-metal на ARM Cortex-M. Мене драйвить саме embedded для БПЛА, тому ваша вакансія — точне попадання. Розумію, що багато чого ще вчити, але швидко навчаюсь і готовий вкладатися.»

---

## 🗺️ Чесна карта навичок (відповідай узгоджено!)

| ✅ Знаю / практикував | 🟡 Вчуся зараз | ⬜ Поки ні (чесно) |
|---|---|---|
| C / C++ база, Python | FreeRTOS (таски, черги) | Zephyr |
| ESP32 (Wi-Fi/BLE), Pico W | STM32 bare-metal | CAN (розумію концепцію) |
| I²C / SPI / UART, GPIO | ARM Cortex-M, HAL/CMSIS | JTAG/SWD руками (розумію призначення) |
| Сенсори: BMP280, IMU, OLED | MAVLink парсинг | Осцилограф / лог. аналізатор (розумію навіщо) |
| Git, Linux CLI/Bash | unit-тести на хості | Комерційний досвід 2 роки |
| Пайка, мультиметр, datasheet | | RTOS на продакшені |

> [!tip] Золоте правило
> На питання, чого не знаєш — **не вигадуй**. Кажи: «Руками поки не робив, але розумію, для чого це і як працює — ось так… Готовий швидко освоїти.» Для defense/БПЛА це перевіряють у перший тиждень.

---

## 📡 Периферія — мусиш знати напам'ять

| | **UART** | **SPI** | **I²C** | **CAN** |
|---|---|---|---|---|
| Синхр.? | Асинхронний | Синхронний | Синхронний | Синхронний (диференц.) |
| Лінії | 2 (TX, RX) | 4 (MOSI, MISO, SCLK, CS) | 2 (SDA, SCL) + pull-up | 2 (CAN_H, CAN_L) |
| Топологія | 1↔1 | 1 master ↔ N slave (по CS) | multi-master, адресація 7-біт | multi-master, шина |
| Швидкість | ~115200 бод | швидкий, МГц+ | 100k/400k/1M | до 1 Mbit, шумостійкий |
| Де | консоль, GPS, телеметрія | сенсори, дисплеї, flash, SD | багато сенсорів на 1 шині | авто, промисловість, дрони |

**Що казати впевнено:** «На ESP32/Pico я підключав сенсори по I²C і SPI, GPS/консоль — по UART. I²C зручний, бо багато пристроїв на двох лініях з адресацією; SPI швидший і full-duplex, але потребує окремий CS на кожен пристрій. UART — найпростіший, асинхронний, обидві сторони мають збігтися по baud rate.»

**Ключові факти-«гачки»:**
- **UART:** немає лінії тактування → потрібен однаковий baud rate. Кадр: start → 8 data → parity? → stop.
- **SPI:** full-duplex, master генерує SCLK, CPOL/CPHA = SPI-режими (0–3).
- **I²C:** напіввдуплекс, потрібні **підтягуючі резистори** на SDA/SCL, кожен пристрій має адресу.
- **CAN:** диференційний → стійкий до завад; пріоритет повідомлень за ID (арбітраж). *Сам не робив — скажи чесно.*

---

## 🧠 Мікроконтролери та ARM Cortex-M

- **MCU** = процесор + пам'ять (Flash для коду, RAM для даних) + периферія на одному кристалі.
- **ARM Cortex-M** (M0/M3/M4/M7) — 32-бітні RISC-ядра, на них збудовані STM32. M4/M7 мають **FPU** (апаратні float).
- **NVIC** — контролер переривань; **SysTick** — системний таймер (тік для RTOS).
- Периферія **memory-mapped** — керуєш регістрами за адресами.
- **ESP32** ≠ Cortex-M: це Xtensa (або RISC-V у нових), але принципи ті самі + Wi-Fi/BLE на борту.

---

## ⚙️ Bare-metal vs RTOS (часте питання!)

| | **Bare-metal** | **RTOS (FreeRTOS)** |
|---|---|---|
| Модель | `while(1)` super-loop + переривання | планувальник + кілька тасків |
| Плюси | просто, детерміновано, мало пам'яті | паралельні задачі, пріоритети, таймінги |
| Мінуси | важко масштабувати логіку | оверхед, складніше |
| Коли | прості пристрої, жорсткий realtime | багато паралельних задач |

**FreeRTOS — мінімум, що треба сказати:**
- **Task** — незалежна функція зі своїм стеком і пріоритетом.
- **Scheduler** — преемптивний за пріоритетом; робить **context switch**.
- **Queue** — потокобезпечна передача даних між тасками.
- **Semaphore** (binary/counting) — сигналізація/синхронізація (напр. ISR → task).
- **Mutex** — захист спільного ресурсу, має **priority inheritance**.
- **ISR-правило:** переривання коротке, без блокувань; усередині — `...FromISR()` API, важку роботу віддаєш у таск через чергу.

---

## 🐛 Прошивка та відладка на реальному залізі

- **Прошивка:** через програматор (ST-Link/J-Link) по **SWD/JTAG**, або бутлоадером по UART/USB.
- **SWD** — 2 лінії (SWDIO, SWCLK), сучасний ARM-дебаг. **JTAG** — старіший, більше пінів.
- **Дебагер** (GDB + OpenOCD): breakpoints, крок, перегляд регістрів/змінних.
- **Осцилограф** — бачиш аналоговий сигнал/таймінг/напругу в часі.
- **Логічний аналізатор** — декодує цифрову шину (SPI/I²C/UART) — видно реальні байти.
- **printf/serial debug** по UART — твій щоденний інструмент (це ти робив!).

> Чесно: «Осцилограф і лог. аналізатор руками ще не використовував, але розумію — осцилограф для аналогових сигналів і таймінгу, лог. аналізатор для декодування протоколів. Свою відладку робив через serial/printf.»

---

## ✅ Тести, документація, Git

- **Unit-тести** в embedded: ганяєш **логіку на хості** (ПК), залізо мокаєш. Інструменти: Unity, Ceedling, CMock, GoogleTest.
- **Інтеграційні:** на реальному залізі / HIL (hardware-in-the-loop).
- **Документація:** README, опис протоколів/регістрів, коментарі — JD це окремо просить.
- **Git:** `clone / branch / commit / push / pull / merge`, PR, вирішення конфліктів. Кажи впевнено — ти це робиш щодня.

---

## 💡 C/C++ дрібниці, які люблять питати

- **`volatile`** — каже компілятору «значення може змінитися ззовні» (регістр, змінна з ISR) → не оптимізуй читання. **Критично в embedded.**
- **`const`** — лише читання.
- **Stack vs Heap:** у embedded `malloc`/heap уникають (фрагментація) → перевага **статичній** алокації.
- **Pointer** — змінна, що містить адресу. Передача по вказівнику = доступ до оригіналу без копії.
- **`#define` vs `const`** / **struct** для регістрів / бітові маски (`|`, `&`, `<<`).

---

## ❓ Питання, які варто поставити їм (показує зрілість)

1. Який стек на проєкті — STM32 bare-metal, FreeRTOS чи Zephyr?
2. Як виглядає менторство для junior на перших місяцях?
3. На якому етапі проєкти — прототип чи серійні вироби?
4. Як організовано тестування прошивки на залізі?
5. Чи передбачено бронювання одразу при оформленні? *(вони згадали — варто уточнити деталі)*

---

## 🚩 Слабкі місця — заготовлені чесні відповіді

- **«2 роки досвіду?»** → «Комерційного — поки немає, я студент 2 курсу. Але маю практичну базу з C/C++ і мікроконтролерів, і JD згадує, що базової підготовки + бажання вчитися достатньо — тому й відгукнувся.»
- **«Zephyr?»** → «З Zephyr не працював. Знайомий з концепцією RTOS через FreeRTOS, який зараз вивчаю; Zephyr освою за тією ж логікою.»
- **«CAN?»** → «Руками не робив. Розумію: диференційна шина, стійка до завад, з арбітражем за пріоритетом — типова для дронів/авто. Готовий освоїти.»
- **«Bronювання / графік навчання?»** → чесно про денну форму + індивідуальний графік на сесії.
